# ── Required: Slack ──────────────────────────────────────────────────
# From https://api.slack.com/apps → your app → OAuth & Basic Information
SLACK_BOT_TOKEN=xoxb-...
SLACK_APP_TOKEN=xapp-...

# Channel where Pagemenot posts incident updates (no # prefix)
PAGEMENOT_CHANNEL=incidents

# ── LLM (pick one) ───────────────────────────────────────────────────
LLM_PROVIDER=openai          # openai | anthropic | ollama
LLM_MODEL=gpt-4o             # gpt-4o | claude-sonnet-4-6 | llama3.1

OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# OLLAMA_URL=http://localhost:11434   # local Ollama

# ── Vector store (embedded — no separate service needed) ─────────────
# ChromaDB runs embedded inside the container. Override if needed.
# CHROMA_PATH=/app/data/chroma

# ── Optional integrations ────────────────────────────────────────────
# Add any. Each one upgrades that tool from mock → live data.
# Skip anything you don't use — mock fallbacks handle the rest.

# ── Metrics / observability ──────────────────────────────────────────
# PROMETHEUS_URL=http://prometheus:9090

# GRAFANA_URL=http://grafana:3000
# GRAFANA_API_KEY=glsa_...

# LOKI_URL=http://loki:3100           # log aggregation (Grafana stack)

# DATADOG_API_KEY=...
# DATADOG_APP_KEY=...
# DATADOG_SITE=datadoghq.com          # or datadoghq.eu, us3.datadoghq.com, etc.

# NEWRELIC_API_KEY=NRAK-...
# NEWRELIC_ACCOUNT_ID=...

# ── Alerting / on-call ───────────────────────────────────────────────
# PAGERDUTY_API_KEY=...
# OPSGENIE_API_KEY=...

# ── Source control / deploy correlation ──────────────────────────────
# GITHUB_TOKEN=ghp_...
# GITHUB_ORG=your-org                 # optional, shortens repo names

# ── Execution ────────────────────────────────────────────────────────
# KUBECONFIG_PATH=/app/.kube/config   # enables kubectl rollback (needs approval)

# ── Misc ─────────────────────────────────────────────────────────────
LOG_LEVEL=INFO
