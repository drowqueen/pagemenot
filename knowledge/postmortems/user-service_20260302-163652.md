# Incident: user-service: all API requests returning 504

**Service:** user-service
**Severity:** critical
**Date:** 2026-03-02
**Resolved automatically:** False
**Triage confidence:** high

## Analysis

### Incident Postmortem Narrative

#### What Happened and Impact:
On [Date], the `user-service` experienced a critical issue where all API requests returned HTTP 504 errors, affecting its functionality across multiple applications that depend on it. This failure led to disruptions in user service availability and impacted various downstream systems, potentially causing delays or failures for end users.

#### Root Cause Analysis:
The root cause of the incident was identified as recent pull request (PR) #1203. This PR introduced a query to filter users based on `last_login_at`, which is not indexed. The unindexed column caused significant performance degradation during high traffic periods, leading to timeout errors and ultimately HTTP 504 responses. The deployment history correlated with the introduction of this change, confirming its role in the failure.

#### Resolution Summary:
Immediate steps were taken to address the issue. Initially, a rollback was performed to revert PR #1203. Following this, a comprehensive index on the `last_login_at` column was created to optimize query performance. Additionally, thorough testing was conducted before any further updates could be merged, ensuring that similar issues would not arise in the future.

#### Prevention Recommendations:
To prevent such incidents from recurring, several preventive measures were implemented:

1. **Automated Indexing Policies**: A policy for automatic indexing of frequently queried fields will be established to ensure that all critical queries are optimized before deployment.
2. **Code Review Enhancements**: The code review process will be enhanced to include specific checks on the impact of new queries and their potential performance implications, particularly focusing on unindexed columns.
3. **Performance Testing Protocols**: Regular performance testing protocols will be introduced for all new features or changes that significantly alter query patterns or data access methods.

By implementing these measures, we aim to maintain high service availability and ensure robustness in our database operations.

## Root Cause

Recent PR #1203 added a query filtering users based on `last_login_at`, which is not indexed, causing performance issues and leading to 504 errors.

## Evidence

- All API requests are returning HTTP 504 errors.
- Recommendation from recommendations: PR #1203 specifically added a query that filters users based on `last_login_at` without an index, causing performance issues.
- Monitoring and diagnosis indicate recent deploy history correlated with the issue.

## Remediation

- [AUTO-SAFE] Check logs for more detailed error messages or patterns
- [AUTO-SAFE] Thoroughly test changes before merging any further updates

## Similar Incidents

- N/A
