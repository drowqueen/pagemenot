# Incident: user-service: all API requests returning 504

**Service:** user-service
**Severity:** critical
**Date:** 2026-03-02
**Resolved automatically:** False
**Triage confidence:** high

## Analysis

**Incident Postmortem Narrative**

### What Happened and Impact:

On 2026-02-26T08:45:00Z, a critical incident occurred in the user-service resulting in all API requests returning a 504 status code. This led to an inability for users to access essential services, causing significant disruption to our customers.

The impact of this incident was far-reaching, with multiple teams and services affected by the outage. Our support team received numerous complaints from customers experiencing issues with user data retrieval and API calls.

### Root Cause Analysis:

Upon investigating the recent deploy history, we identified that PR #1203 introduced a KeyError in the Stripe webhook handler during Deploy #4521. This critical error caused the gateway to timeout, resulting in failed user data retrieval attempts and subsequent 504 errors for all API requests.

Further analysis revealed that the issue was exacerbated by the addition of query filtering users by `last_login_at` to the user search functionality, which inadvertently created a resource-intensive operation that strained the system's capacity.

### Resolution Summary:

After conducting an extensive investigation, we identified the root cause and implemented an emergency patch to resolve the KeyError in the Stripe webhook handler. This fix was deployed at 2026-02-26T14:30:00Z, followed by thorough testing and validation to ensure the stability of our systems.

The auto-resolved procedure for 'user-service API requests returning 504' was successfully triggered during this incident, demonstrating the effectiveness of our SRE knowledge base in automating resolution procedures.

### Prevention Recommendations:

To prevent similar incidents from occurring in the future, we recommend the following changes:

1.  **Enhance testing and validation**: Implement more comprehensive testing for new PRs, focusing on identifying potential errors in high-traffic areas like webhooks.
2.  **Improve error handling and logging**: Enrich log entries to capture detailed information about gateway timeouts and failed user data retrieval attempts, facilitating quicker root cause identification.
3.  **Re-evaluate resource-intensive operations**: Regularly review system performance and optimize resource-hungry queries to prevent strain on our infrastructure.

By implementing these measures, we can significantly reduce the likelihood of future occurrences like this critical incident and ensure a more stable experience for our customers.

## Root Cause

Deploy #4521 introduced a KeyError in the Stripe webhook handler.

## Evidence

- Recent deploy history shows PR #1203 was merged on 2026-02-26T08:45:00Z, which added a query filtering users by `last_login_at` to the user search functionality.
- Log entries show WARN and ERROR messages related to gateway timeouts and failed user data retrieval due to gateway timeouts.

## Remediation

- [AUTO-SAFE] Search operational runbooks for fix procedures for 'user-service API requests returning 504'

## Similar Incidents

- N/A
